name: Price Tracker

on:
  workflow_dispatch:
  schedule:
    # Tre controlli al giorno in fascia diurna: 9, 15 e 21 UTC.
    # In Italia (CET/CEST) equivalgono a 10/11-16/17-22/23 locali,
    # quindi la notte (00-08) resta coperta dalla sospensione.
    - cron: '0 9,15,21 * * *'

# Impedisce che più run si sovrappongano: se parte una nuova esecuzione
# mentre una è già in corso, quella in corso viene annullata. Evita
# chiamate API duplicate (ogni run consuma 1 credito ScraperAPI per prodotto).
concurrency:
  group: price-tracker
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          python -m unittest discover -s tests -v

  check-prices:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Download previous state
        uses: actions/download-artifact@v4
        with:
          name: price-tracker-state
          path: ./
        continue-on-error: true

      - name: Run price tracker
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SCRAPERAPI_API_KEY: ${{ secrets.SCRAPERAPI_API_KEY }}
          PRICE_STATE_FILE: state.json
        run: |
          python main.py

      # Persisto lo stato delle notifiche tra una run e l'altra (dedup alert).
      # L'upload gira anche se la run fallisce, così lo stato non va perso.
      - name: Upload state
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: price-tracker-state
          path: state.json
          if-no-files-found: ignore
          overwrite: true
